<Window x:Class="WpfApp1.Views.AddItemsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Style="{StaticResource ModernWindow}"
        Title="添加项目" Height="420" Width="480"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
    <Border Background="{StaticResource BgSecondaryBrush}"
            BorderBrush="{StaticResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="12"
            Padding="24">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="添加项目" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,16"/>

            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
                <Button Content="选择文件..." Style="{StaticResource ModernButton}" Click="PickFiles_Click"/>
                <Button Content="选择文件夹..." Style="{StaticResource ModernButton}" Margin="8,0,0,0" Click="PickFolders_Click"/>
            </StackPanel>

            <Border Grid.Row="2"
                    Background="{StaticResource BgTertiaryBrush}"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1"
                    CornerRadius="8">
                <ListBox x:Name="PathsList"
                         Background="Transparent"
                         BorderThickness="0"
                         Foreground="{StaticResource TextPrimaryBrush}"
                         Margin="4"/>
            </Border>

            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                <Button Content="取消" Style="{StaticResource ModernButton}" Width="80" Margin="0,0,8,0" Click="Cancel_Click"/>
                <Button Content="添加" Style="{StaticResource AccentButton}" Width="80" Click="Ok_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
